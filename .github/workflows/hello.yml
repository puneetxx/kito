name: test
on: [push , workflow_dispatch]
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
          - name : get code
            run: |
              echo "cloning code"
              echo "${{toJson(github)}}" 
              git clone https://github.com/${{github.repository}}.git